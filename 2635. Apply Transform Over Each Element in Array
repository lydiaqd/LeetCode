/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function (arr, fn) {
    const newArray = [];

    arr.forEach((value, index) => {
        newArray.push(fn(value, index));
    });
        return newArray;
};