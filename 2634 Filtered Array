/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var filter = function(arr, fn) {
    filteredArray[]
    if fn(arr)[i]) is true push to filteredArray
    return filteredArray
};