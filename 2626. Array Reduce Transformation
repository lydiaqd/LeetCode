/**
 * @param {number[]} nums
 * @param {Function} fn
 * @param {number} init
 * @return {number}
 */
 
var reduce = function(nums, fn, init) {
    if array length = 0
    return init
    else

    for array.length 
    add nums
    return init
};